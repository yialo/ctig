mixin basic-heading(basicHeading_type)
  case basicHeading_type
    when 'h1'
      h1.basic-heading(class=attributes.class)
        block
    when 'h2'
    default
      h2.basic-heading(class=attributes.class)
        block
