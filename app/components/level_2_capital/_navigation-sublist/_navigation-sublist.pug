mixin navigation-sublist(navigationSublist_data)
  ul.navigation-sublist(class=attributes.class)
    each item in navigationSublist_data
      - const [text, path] = item;
      li.generic-textblock.navigation-sublist__item
        a.navigation-sublist__link(href=path)= text
