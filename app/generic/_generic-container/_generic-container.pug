mixin generic-container()
  -
    const genericContainer_modifier = _IS_MAINPAGE
      ? 'generic-container--mainpage'
      : null;
  .generic-container(class=[genericContainer_modifier, attributes.class])
    block
